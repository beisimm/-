<view class='content'>
  <view class='screen'>
    {{screenData}}
  </view>
  <view class='btnGroup'>
    <view class='item orange' id="{{id1}}" bindtap='clickButton'>退格</view>
    <view class='item orange' id="{{id2}}" bindtap='clickButton'>清屏</view>
    <view class='item orange' id="{{id3}}" bindtap='clickButton'>+/-</view>
    <view class='item orange' id="{{id4}}" bindtap='clickButton'>+</view>
  </view>
  <view class='btnGroup'>
    <view class='item blue' id="{{id5}}" bindtap='clickButton'>9</view>
    <view class='item blue' id="{{id6}}" bindtap='clickButton'>8</view>
    <view class='item blue' id="{{id7}}" bindtap='clickButton'>7</view>
    <view class='item orange' id="{{id8}}" bindtap='clickButton'>-</view>
  </view>
  <view class='btnGroup'>
    <view class='item blue' id="{{id9}}" bindtap='clickButton'>6</view>
    <view class='item blue' id="{{id10}}" bindtap='clickButton'>5</view>
    <view class='item blue' id="{{id11}}" bindtap='clickButton'>4</view>
    <view class='item orange' id="{{id12}}" bindtap='clickButton'>×</view>
  </view>
  <view class='btnGroup'>
    <view class='item blue' id="{{id13}}" bindtap='clickButton'>3</view>
    <view class='item blue' id="{{id14}}" bindtap='clickButton'>2</view>
    <view class='item blue' id="{{id15}}" bindtap='clickButton'>1</view>
    <view class='item orange' id="{{id16}}" bindtap='clickButton'>÷</view>
  </view>
  <view class='btnGroup'>
    <view class='item blue' id="{{id17}}" bindtap='clickButton'>0</view>
    <view class='item blue' id="{{id18}}" bindtap='clickButton'>.</view>
    <view class='item blue' id="{{id19}}" bindtap='clickButton'>历史</view>
    <view class='item orange' id="{{id20}}" bindtap='clickButton'>=</view>
  </view>
</view>